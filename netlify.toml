[build]
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Note: This is a static HTML site with serverless functions
# No build step needed - just deploy the HTML and functions